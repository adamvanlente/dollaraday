div.dad-form
  | add a new savings goal

  // For form
  // Values must be ints
  // Date must be in the future

  form(action="/newgoal", method="post")
    input(name="dad-form-goal-name", placeholder="goal name")
    input(name="dad-form-goal-target-amount", placeholder="goal amount")
    input(name="dad-form-goal-amount-saved", placeholder="goal amount saved")
    input(name="dad-form-goal-target-date", placeholder="target date", id="dad-form-goal-target-date")
    input(type="radio", name="dad-form-email-alerts", value="on", checked="true")
    label
      | on
    input(type="radio", name="dad-form-email-alerts", value="off")
    label
      | off
    input(type="hidden", value="#{user._id}", name="dad-form-user")

    button(type="submit")
      | add new goal
